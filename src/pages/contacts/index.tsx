import { Form } from "../../components/form/form"
import Layout from "../../layout/layout"

const Contacts = () => {
  return (
    <Layout>
      <Form />
    </Layout>
  )
}

export default Contacts